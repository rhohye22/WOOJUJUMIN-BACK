<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
	<mapper namespace="com.woojujumin.dao.PartyRequestDao">
	
	<select id="myRequestList" resultType="com.woojujumin.dto.ApplyDto" parameterType="com.woojujumin.dto.mypartyBbsParam">
		select *
		from 
			(select row_number()over(order by wdate desc) as rnum,
			 partySeq, applyMem, check, wdate
		from apply
		where 1=1 AND applyMem =#{id}
			
			order by wdate desc) a
			
		where applyMem =#{id} AND rnum between ${start} and ${end} 
	</select>
		
		
		<select id="getmyRequestList" parameterType="com.woojujumin.dto.mypartyBbsParam" resultType="Integer">
	
		select ifnull(count(*), 0)
		from apply
		
	
	</select>
	</mapper>